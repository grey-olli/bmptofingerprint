Тестовая задача.

По приложенному файлу получить шаблон стандарта ISO/IEC 19794-2.

Подсказка: все исходники есть в интернете, нужно только правильно собрать.

Выходной результат — скомпилированная утилита созданная с использованием gcc c, со следующей командной строкой:

fingerprint <infile.bmp> <outfile.tmp>

